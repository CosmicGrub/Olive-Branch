name: olive_client
description: Olive Branch client — child and guardian shells.
publish_to: none
version: 0.15.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
